{% extends 'base.html.twig' %}

{% block title %}Modifier Terrain{% endblock %}

{% block body %}
<div class="container px-5 my-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card border-0 rounded-3 shadow-lg">
                <div class="card-body p-4">
                    <div class="text-center">
                    <h1 class="text-center text-primary mb-3">Modifier terrain</h1>
                    {{ include('admin_terrain/_form.html.twig', {'button_label': 'Modifier'}) }}
                    {{ include('admin_terrain/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
        <a href="{{ path('app_admin_terrain_index') }}" class="btn btn-outline-primary btn-sm shadow">Retour a la liste</a>
</div>

    




{% endblock %}
