{% extends 'base.html.twig' %}

{% block title %}Parametre profil{% endblock %}

{% block body %}
    <h1 class="text-center">Profil</h1>

<div class="container-fluid">
  <div class="table-responsive">
    <table id="table" class="table table-striped table-hover table-bordered text-center" >
        <thead class="table-primary" >
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Adresse</th>
                <th>Tel</th>
                <th>Mise à jour</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.Nom }}</td>
                <td>{{ user.Prenom }}</td>
                <td>{{ user.Adresse }}</td>
                <td>{{ user.Tel }}</td>
                <td>
                    
                    <a href="{{ path('app_profil_edit', {'id': user.id}) }}" class="btn btn-warning btn-sm shadow">Modifier</a>
                    {# {{ include('profil/_delete_form.html.twig') }} #}
                </td>
            </tr>
        </tbody>
    </table>
  </div>
  <a href="{{ path('home_swish') }}" class="btn btn-outline-primary btn-sm shadow">Retour</a>
</div>


    
{% endblock %}
