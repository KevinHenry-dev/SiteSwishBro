{% extends 'base.html.twig' %}

{% block title %}Gestion événement{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h1 class="text-center my-3 display-5">Gestion évenement</h1>

            <div class="container mt-5">
                <div class="row justify-content-center">
                    <div class="col-md-6">
                        {{ include('calendar/_form.html.twig', {'button_label': 'Mise à jour '}) }}
                    </div>
                </div>
                
                
                <div class="row justify-content-center mt-3">
                    <div class="col-md-6">
                        {{ include('calendar/_delete_form.html.twig') }}
                    </div>
                </div>
                <div class="row justify-content-center mt-3">
                    <div class="col-md-6">
                        <a href="{{ path('app_calendar_show') }}" class="btn btn-outline-primary shadow">Retour</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}